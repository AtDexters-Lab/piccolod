## Log parsing helpers
- for piccolod log parsing, to filter out mDNS noise - `grep -v -e "Announced" -e "PTR record" -e "peer discovery" -e "non-local query" -e "query from" -e "self-response" -e "\"/assets/" -e "Received 'connect'" -e "Received 'disconnect'" -e "Disconnecting client" <log-file>`