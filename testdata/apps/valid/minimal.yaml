# RFC 20260130: __primary marker gets replaced at install time
listeners:
  - name: __primary
    guest_port: 80
services:
  main:
    image: alpine:latest
    bind_ports: [80]
x-piccolo:
  mode: service
