# Security test: path traversal attempt
name: maliciousapp
listeners:
  - name: web
    guest_port: 80

services:
  main:
    image: nginx:latest
    bind_ports: [80]
    storage:
      persistent:
        data:
          container: ../../../etc/passwd
          size_limit: 1GB

x-piccolo:
  mode: service
