name: alpine-env-test
type: user
listeners:
  - name: web
    guest_port: 80
services:
  main:
    image: docker.io/library/alpine:latest
    bind_ports: [80]
    environment:
      TEST_MODE: integration
      DEBUG_LEVEL: "3"
      APP_CONFIG: /etc/app/config.json
x-piccolo:
  mode: service
